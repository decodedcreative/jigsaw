{
  "name": "jigsaw",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "changeset": "changeset",
    "generate-changeset": "node scripts/generate-changeset-from-changes.mjs",
    "release:patch": "node scripts/generate-changeset-from-changes.mjs --bump patch --force",
    "release:minor": "node scripts/generate-changeset-from-changes.mjs --bump minor --force",
    "release:major": "node scripts/generate-changeset-from-changes.mjs --bump major --force",
    "version-packages": "changeset version",
    "release": "npm run validate:packages && changeset publish",
    "verify:packages": "npm run build --workspace=@jigsaw-ds/design-system --workspace=@jigsaw-ds/tokens --workspace=@jigsaw-ds/theme-default --workspace=@jigsaw-ds/theme-portfolio && node scripts/verify-publishable-packages.mjs",
    "validate:packages": "npm run verify:packages && node scripts/validate-publishable-packages.mjs",
    "test:release-notes": "node --test scripts/github-release-notes.test.mjs",
    "test:generate-changeset": "node --test scripts/generate-changeset-from-changes.test.mjs scripts/lib/publishable-packages.test.mjs"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.4",
    "@changesets/cli": "^2.31.0",
    "prettier": "^3.2.5",
    "publint": "^0.3.21",
    "turbo": "^2.3.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/themes/*"
  ],
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=20.19.0"
  }
}
