---
"@jigsaw-ds/design-system": patch
---

Add configureTwMerge / configureTheme for RSC-safe app-wide class-name merge injection, and document ThemeProvider as optional nested client overrides only.
