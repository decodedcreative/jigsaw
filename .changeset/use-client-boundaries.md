---
"@jigsaw-ds/design-system": patch
---

Publish per-module builds so `"use client"` stays on interactive leaves only — RSC-safe presentational imports no longer inherit a package-wide client boundary.
