/**
 * @jigsaw/tokens — design tokens via Style Dictionary (Jigsaw)
 *
 * Consume tokens as:
 * - CSS variables: import "@jigsaw/tokens/tokens.css" then use var(--color-brand-primary), etc.
 * - Tailwind theme: import theme from "@jigsaw/tokens/theme" and spread into theme.extend in tailwind.config
 */
export const tokensPackageVersion = "0.0.1";
